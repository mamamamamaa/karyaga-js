"use strict";

// Перепишіть визначення функції add() використовуючи синтаксис Function Expression.

// function add(x, y) {
//   return x + y;
// }

var add = function (x, y) {
  return x + y;
};

console.log(add(4, 9));
